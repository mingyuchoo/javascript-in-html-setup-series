{
  "name": "01-using-es5-in-html",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "eslint --config .eslintrc.yml --fix .",
    "test:jest": "jest --config=jest.config.json",
    "test:mocha": "nyc --reporter=html --reporter=text mocha --config=.mocharc.yml",
    "live": "live-server --open=public",
    "sass": "node-sass --watch src/scss -o public/css",
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^7.1.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.3",
    "jest": "^26.0.1",
    "live-server": "^1.2.1",
    "mocha": "^7.2.0",
    "node-sass": "^4.14.1",
    "nyc": "^15.0.1",
    "prettier": "^2.0.5",
  }
}

const sum = (a, b) => {
  return a + b;
};

const hello = (name) => {
  document.getElementById('output').innerHTML = 'Hello, ' + name;
};

const msg = () => {
  alert('Hello, JavaScript');
};
//export default sum;
